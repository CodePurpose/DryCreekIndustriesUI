import styled from "styled-components";
import { Background } from "../globalStyles";

export const CodePurposeFooterBackground = styled(Background)`
  text-align: center;
  padding: 15px 0;
  color: white;

  > p {
    margin: 0;
    > a {
      color: white;
    }
  }
`;
